# content of pytest.ini
[pytest]
; 指定命令行选项 等价于 pytest -rsxX -q test_main.py
;addopts = -rsxX -q
addopts = -v -s

# 忽略的目录和文件
norecursedirs = \.* *.pyc svn _build tmp*

# 指定测试类格式
python_classes = Test*

# 指定测试函数的格式
python_functions = test_*


